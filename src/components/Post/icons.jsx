import { menu_icon, menu_like, menu_comment, menu_share, menu_save, menu_emoji } from 'src/components/Icons'

export const icons = {
  menu: menu_icon(),
  like: menu_like(),
  comment: menu_comment(),
  share: menu_share(),
  save: menu_save(),
  emoji: menu_emoji()
}